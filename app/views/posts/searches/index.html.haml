= render partial: "header"
.none
  .contents.row
    - @posts.each do |post|
      = render partial: "post", locals: { post: post }
    - @articles.each do |article|
      = link_to title(article), url(article), class: "top--title"
